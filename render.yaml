services:
  - type: web
    name: churn-prediction
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    plan: free
    envVars:
      - key: USE_REGISTRY
        value: "false"
